In order to launch the project, run:

bash compile.sh

Then:
cd  bin/java/classes/java/main

And finally launch the server and 2 players accoring to the specification, for example:
java Server
java Client
