plugins {
    id 'java'
}

sourceSets {
    main.java.srcDirs = [ '.' ]
}

buildDir = '../../bin'

repositories {
    mavenCentral()
}

copy {
    from '../../src/cpp/assets/'
    into "${buildDir}/classes/java/main/assets/"
}
